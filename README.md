# prox-sensor-testing
This repository contains a Python script designed to extract specific, structured data from multi-tab Excel files and compile it into a single, clean CSV file. It's ideal for automating the reporting of test results or configuration parameters where data is stored in the first column of numerous sheets.
